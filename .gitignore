# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc 

# Command scripts (keep local)
*.sh

# Data
LibriSpeech/

# Bridge2AI local exports (large wav trees)
bridge2ai_adult_wav*/
bridge2ai_pediatric_wav*/
bridge2ai_adult_wav*.log

# Test files / old files
conf_old.py 
conf_test.py
inference-new.py 
train_ae.py
train_ctc.py

# Trained models
trained_model/

# Misc.
paper/
runs/
.cache/

.venv/
.hf_cache/
Glottal_signals_db/
classification_results/
.idea/
Vowels/

# macOS
.DS_Store

# Generated feature tables / artifacts
features_*.csv
tmp_features/

# Frame-level glottal CSVs (often 100MB+; GitHub blocks them — keep on /stek, S3, or LFS)
*framelevel*.csv

# Non-framelevel but still large glottal CSV exports (keep local / external storage)
glottal_features_*QCP*_python_tuned*.csv
glottal_features_test_*.csv

# Training/inference artifacts
*.npz
trained_model*/

# Whisper fine-tuning outputs
whisper_runs/

# Generic logs / predictions
*.log
*.jsonl

# Bridge2AI Zipformer build outputs (keep manifests + subset_meta.tsv)
bridge2ai_zipformer_*/mels/
bridge2ai_zipformer_*/trained_model*/
bridge2ai_zipformer_*/logs/
bridge2ai_zipformer_*/wer_*.json

# Keep the split definitions and metadata
!bridge2ai_zipformer_full_all_tasks/subset_meta.tsv
!bridge2ai_zipformer_full_all_tasks/manifests/
!bridge2ai_zipformer_full_all_tasks/manifests/*.txt

# Plots generated from logs
loss_total_*.csv
loss_total_*.png

# Slide / report exports
*.html

# Large archives / downloads
*.tar.gz

# Local datasets (raw corpora)
German/
Czech/

# Colombian DDK raw audio (local folder; contains many .wav files)
DDK analysis/

# Presentation assets (generated figures/tables)
presentation_assets/
